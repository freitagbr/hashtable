#ifndef HASH_H
#define HASH_H

#include "prime.h"
#include "debug.h"

#include <string.h>
#include <math.h>

int ht_hash(const char*, const int, const int);

int ht_get_hash(const char*, const int, const int);

#endif // HASH_H
